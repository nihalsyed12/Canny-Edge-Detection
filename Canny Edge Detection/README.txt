#101107401
run pip3 install opencv-python
run pip3 install numpy 

run script with images in the same directory, script will 
perform operations and write new images into directory including:
gradient magnitude image
gradient orientation image 
non-max suppression image 